# Copilot Instructions

## Project Guidelines
- The repository should retain the successfully deployed SQL Server 2.0.3 implementation as the canonical standalone SSMS upgrade: run the entire script in the selected existing database, without USE, CREATE DATABASE, SQLCMD variables, or GO; preserve legacy data and use the documented transactional phases and validations. Retire superseded SQL scripts rather than keeping duplicate deployment paths.